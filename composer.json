{
    "name": "vncompare/api",
    "type": "project",
    "description": "VNCompare API - Vietnam Paint Comparison Platform",
    "license": "MIT",
    "require": {
        "php": "^8.2"
    },
    "autoload": {
        "psr-4": {
            "App\\": "apps/api/src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "cd apps/api && composer install --no-dev --optimize-autoloader"
        ]
    }
}
